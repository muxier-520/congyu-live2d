# 丛雨 Live2D — 项目大纲

---

## 项目概述

丛雨（Murasame）是一个桌面 AI 聊天伴侣应用，结合 **Live2D 虚拟形象**、**AI 对话**（OpenClaw Gateway / Ollama）和 **GPT-SoVITS 语音合成**。前端基于纯 HTML/CSS/JS，后端 Node.js 原生 http 模块，打包为 Electron 桌面应用。

---

## 一、目录结构

```
E:\openclaw\murasame\
├── src/                          # ★ 主开发目录
│   ├── server.js                 # Node.js 后端 (v3.2, 877 行)
│   ├── index.html                # 主前端页面 (44KB)
│   ├── config.json               # 运行时配置 (v3.1)
│   ├── package.json              # 包元数据
│   ├── electron/
│   │   ├── main.js               # Electron 主进程 (v2.2)
│   │   └── preload.js            # 预加载桥接
│   ├── css/
│   │   ├── style.css             # 主样式 — 樱花紫玻璃拟态主题
│   │   └── galgame.css           # GalGame 视觉小说模式样式
│   ├── js/
│   │   ├── app.js                # 前端主逻辑 (Live2D + 聊天 UI)
│   │   ├── openclaw-features.js  # 模型选择器、技能、记忆面板
│   │   ├── chat-config.js        # 对话模式配置
│   │   ├── galgame.js            # 视觉小说覆盖层
│   │   └── gateway_ws.js         # (已废弃) WebSocket 客户端
│   ├── models/                   # Live2D 模型 (Cubism 3)
│   │   ├── Murasame.model3.json  # 模型入口
│   │   ├── Murasame.moc3         # 几何数据
│   │   ├── Murasame.4096/        # 纹理图集 (4K)
│   │   ├── exp/                  # 7 组表情
│   │   ├── motion/               # 12 组动作
│   │   └── sounds/               # 参考音频 + 语音样本
│   └── audio/                    # TTS 输出目录 (运行时生成)
│
├── launcher/                     # ★ Electron 打包运行环境
│   ├── 丛雨Live2D.exe            # 主可执行文件
│   ├── *.dll / *.pak / *.bin     # Chromium 运行时
│   └── resources/
│       ├── app.asar              # 当前活动应用包 (216MB)
│       ├── app.asar.unpacked/    # 解包文件 (electron/ 目录)
│       ├── app_current/          # 开发/修复工作副本
│       ├── app_extract/          # 从 asar 解压的副本
│       ├── app_extract_step2/    # 分步解压副本
│       ├── app_check/            # 验证副本
│       ├── app_verify/           # 验证副本
│       └── server.js             # 独立后端副本 (已提取)
│
├── model/                        # ★ AI 语音模型权重
│   ├── congyu-e15.ckpt           # GPT 语音模型 (148MB)
│   ├── congyu_e8_s200.pth       # SoVITS 语音模型 (128MB)
│   ├── s2Dv2ProPlus.pth          # SoVITS 解码器 (120MB)
│   └── pretrained_eres2netv2w24s4ep4.ckpt  # 说话人编码器 (102MB)
│
├── start.py                      # [推荐] Python 总启动器
├── start.ps1 / 启动.ps1          # PowerShell 启动器
├── start.bat / 启动.bat          # 批处理启动
├── stop.ps1 / 停止.ps1           # 停止脚本
├── start_gptsovits.bat / .py     # 单独启动 GPT-SoVITS
│
└── 丛雨项目结构文档.md            # 项目内部文档
```

---

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                    Electron Shell                             │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  BrowserWindow (http://localhost:8888)               │    │
│  │  ┌────────────┐  ┌────────────────────────────────┐  │    │
│  │  │ Live2D     │  │  Chat UI (index.html)          │  │    │
│  │  │ Canvas     │  │  ┌─────┐ ┌──────┐ ┌─────────┐│  │    │
│  │  │ (Pixi.js)  │  │  │消息  │ │表情栏│ │快捷操作 ││  │    │
│  │  └────────────┘  │  └─────┘ └──────┘ └─────────┘│  │    │
│  └──────────────────┴────────────────────────────────┘  │    │
│                          │ HTTP                           │    │
│  ┌───────────────────────┴───────────────────────────┐    │    │
│  │  server.js (fork 子进程, 127.0.0.1:8888)         │    │    │
│  │                                                   │    │    │
│  │  ┌─────────────┐  ┌──────────────┐  ┌─────────┐  │    │    │
│  │  │ 静态文件服务  │  │ 聊天代理      │  │ TTS 引擎│  │    │    │
│  │  │ (HTML/CSS/  │  │ → Gateway    │  │ → GPT-  │  │    │    │
│  │  │  JS/模型)    │  │ → Ollama     │  │   SoVITS│  │    │    │
│  │  └─────────────┘  └──────┬───────┘  └─────┬────┘  │    │    │
│  └──────────────────────────┼────────────────┼───────┘    │    │
└─────────────────────────────┼────────────────┼────────────┘    │
                              │                │                  │
                   ┌──────────▼──────┐  ┌──────▼──────────┐      │
                   │ OpenClaw Gateway│  │  GPT-SoVITS     │      │
                   │ :28789          │  │  :9880           │      │
                   └─────────────────┘  └─────────────────┘      │
                              │                                   │
                   ┌──────────▼──────┐                           │
                   │ Ollama          │                           │
                   │ :11434          │                           │
                   └─────────────────┘                           │
```

---

## 三、核心技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| **桌面壳** | Electron 28 | 1400x900 窗口，系统托盘，子进程管理 |
| **前端** | 原生 HTML/CSS/JS | 无框架，Pixi.js v7 + pixi-live2d-display |
| **后端** | Node.js http 模块 | 无 Express，纯原生路由 |
| **AI 对话** | OpenClaw Gateway / Ollama | 可选后端，system prompt 注入 |
| **语音合成** | GPT-SoVITS v2 Pro | 本地推理，自定义角色音色 |
| **虚拟形象** | Live2D Cubism 3 | Pixi.js 渲染，鼠标追踪，表情切换 |
| **打包** | asar + electron-builder | NSIS 安装包 |

---

## 四、后端 API 端点 (server.js)

| 方法 | 路由 | 功能 |
|------|------|------|
| `GET` | `/api/health` | 服务器健康检查 (GPU/TTS/Ollama 状态) |
| `GET` | `/api/tts-latest` | 获取最新 TTS 音频 URL |
| `POST` | `/api/tts` | TTS 生成（返回原始音频） |
| `POST` | `/api/tts-generate` | TTS 生成（返回 JSON `{audio_url}`） |
| `GET/POST` | `/api/tts-config` | 获取/更新 TTS 参数 |
| `GET/POST` | `/api/tts-mode` | 切换 TTS 模式 (local/system/cloud) |
| `GET/POST` | `/api/gpt-sovits-config` | 获取/更新 GPT-SoVITS 连接配置 |
| `GET/POST` | `/api/chat-mode` | 切换聊天模式 |
| `GET` | `/api/ref-audio-list` | 列出参考音频文件 |
| `POST` | `/api/upload-file` | 文件上传 (图片/文档) |
| `POST` | `/api/upload-audio` | 音频上传 |
| `GET/POST/DELETE` | `/api/memory` | 记忆库 CRUD |
| `POST` | `/api/memory/pin` | 置顶记忆 |
| `POST` | `/api/memory/extract` | AI 自动提取记忆 |
| `GET` | `/api/memory/search` | 关键词搜索记忆 |
| `GET` | `/api/memory/conversations` | 对话历史列表 |
| `POST` | `/api/gateway/v1/chat/completions` | → 代理到 OpenClaw Gateway |
| `POST` | `/api/ollama/v1/chat/completions` | → 代理到本地 Ollama |
| `GET` | `/audio/*` | 静态音频文件服务 |

---

## 五、配置系统 (config.json v3.1)

```jsonc
{
  "server":    { "port": 8888, "host": "127.0.0.1" },
  "gateway":   { "url": "http://127.0.0.1:28789", "token": "..." },
  "ollama":    { "model": "qwen2.5:latest", "port": 11434 },
  "gpt_sovits":{ "hostname": "127.0.0.1", "port": 9880 },
  "tts":       { "mode": "local", "ref_audio": "...", "text_lang": "auto" },
  "models":    [ /* 4 个预设 AI 后端 */ ],
  "system_prompt": "你是丛雨(Murasame)，一个温柔可爱的日式女仆..."
}
```

---

## 六、启动方式

| 方式 | 命令 | 启动的服务 |
|------|------|-----------|
| **Python 启动** (推荐) | `python start.py` | Ollama + GPT-SoVITS + Node 服务器 |
| **PowerShell** | `.\启动.ps1` | GPT-SoVITS + Electron |
| **直接运行 exe** | `丛雨Live2D.exe` | Electron (内嵌 fork server.js) |
| **开发模式** | `node server.js` + 浏览器 | 仅后端，浏览器访问 `:8888` |

---

## 七、关键特性

1. **Live2D 虚拟形象** — 全屏渲染，鼠标追踪视线，点击互动，7 种表情 + 12 组动作
2. **AI 对话** — 支持 OpenClaw Gateway 和 Ollama 两种后端，system prompt 注入角色设定
3. **语音合成 (TTS)** — GPT-SoVITS 本地推理，自定义音色，队列管理
4. **记忆系统** — 文件持久化存储，关键词检索，对话中自动注入相关记忆到 system prompt
5. **玻璃拟态 UI** — 樱花紫主题，毛玻璃效果，深色渐变背景，樱花飘落 + 光晕粒子特效
6. **GalGame 模式** — 视觉小说风格覆盖层，可切换
7. **Electron 桌面集成** — 系统托盘，子进程管理，自动启动外部服务

---

## 八、已知问题

- 源码有三份副本 (`src/` / `app_current/` / `app_extract/`) **不同步**，改源码后需运行 `重新打包.bat` 更新 asar
- `gateway_ws.js` (WebSocket) 已废弃但未清理
- 技能/日程/提醒/记忆面板的前端 UI 部分功能为存根，未连接后端
- 记忆检索基于**关键词匹配**，非语义向量搜索
