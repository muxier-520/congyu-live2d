# 丛雨 Live2D — 项目大纲

---

## 项目概述

丛雨（Murasame）是一个桌面 AI 聊天伴侣应用，结合 **Live2D 虚拟形象**、**AI 对话**（OpenClaw Gateway / Ollama）和 **GPT-SoVITS 语音合成**。前端基于纯 HTML/CSS/JS，后端 Node.js 原生 http 模块，打包为 Electron 桌面应用。

---

## 一、目录结构

```
E:\openclaw\murasame\
│
├── launcher/                        # ★ Electron 桌面应用
│   ├── 丛雨Live2D.exe              # 主可执行文件
│   ├── *.dll / *.pak / *.bin       # Chromium 运行时
│   └── resources/
│       ├── app.asar                 # 打包的应用包
│       ├── app.asar.unpacked/       # 解包文件 (electron/)
│       └── app_current/             # ★ 主开发目录
│           ├── server.js            # Node.js 后端入口 (v4.0, 模块化)
│           ├── index.html           # 主前端页面
│           ├── config.json          # 运行时配置 (v3.0)
│           ├── package.json         # 包元数据
│           ├── lib/                 # 基础库
│           │   ├── config.js        # 配置加载/合并/保存
│           │   ├── logger.js        # 分级日志 + 环形缓冲区
│           │   └── utils.js         # HTTP 工具、端口管理、限流
│           ├── services/            # 业务服务层
│           │   ├── tts.js           # GPT-SoVITS 客户端 + 进程管理 + GPU 缓存
│           │   └── ai.js            # Ollama 进程管理 + Gateway/Ollama 代理
│           ├── routes/              # HTTP 路由层
│           │   ├── tts.js           # TTS 端点
│           │   ├── chat.js          # 对话代理端点
│           │   └── admin.js         # 管理端点 (健康检查、GC、Bug 报告)
│           ├── electron/
│           │   ├── main.js          # Electron 主进程
│           │   └── preload.js       # 预加载桥接
│           ├── css/
│           │   ├── style.css        # 主样式 — 樱花紫玻璃拟态主题
│           │   └── galgame.css      # GalGame 视觉小说模式样式
│           ├── js/
│           │   ├── app.js           # 前端主逻辑 (Live2D + 聊天 UI)
│           │   ├── openclaw-features.js  # 模型选择器、技能、记忆面板
│           │   ├── chat-config.js        # 对话模式配置
│           │   ├── chat-config-simple.js # 简化版配置 UI
│           │   └── galgame.js            # 视觉小说覆盖层
│           ├── models/              # Live2D 模型 (Cubism 3)
│           │   ├── Murasame.model3.json  # 模型入口
│           │   ├── Murasame.moc3         # 几何数据
│           │   ├── Murasame.4096/        # 纹理图集 (4K)
│           │   ├── exp/                  # 7 组表情
│           │   └── motion/               # 12 组动作
│           ├── models/sounds/        # 参考音频
│           ├── ref_audio/            # 参考音频备选
│           ├── audio/                # TTS 输出 (运行时生成)
│           ├── bug_reports/          # Bug 报告 (运行时生成)
│           ├── 启动浏览器版.bat       # 开发启动 — 浏览器模式
│           ├── 启动桌面版.bat         # 开发启动 — 桌面浏览器
│           ├── 启动丛雨.bat          # 完整启动 (GPT-SoVITS + 服务器)
│           ├── 停止服务.bat          # 停止服务器
│           └── 重新打包.bat          # 构建 asar + electron-builder
│
├── model/                           # ★ AI 语音模型权重
│   ├── congyu-e15.ckpt             # GPT 语音模型
│   ├── congyu_e8_s200.pth          # SoVITS 语音模型
│   ├── s2Dv2ProPlus.pth             # SoVITS 解码器
│   └── pretrained_eres2netv2w24s4ep4.ckpt  # 说话人编码器
│
├── config.json                      # ★ 根配置 (v3.1, 含 models 数组)
├── start.py                         # Python 总启动器
├── start.ps1                        # PowerShell 启动器
├── start.bat                        # 批处理启动
├── stop.bat / stop.ps1              # 停止脚本
├── start_gptsovits.bat / .py        # 单独启动 GPT-SoVITS
├── 丛雨项目结构文档.md               # 项目内部文档
├── README.md                        # 项目说明
└── output/                          # 构建输出目录
```

---

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                      Electron Shell                           │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  BrowserWindow (http://localhost:8888)               │    │
│  │  ┌────────────┐  ┌────────────────────────────────┐  │    │
│  │  │ Live2D     │  │  Chat UI (index.html)          │  │    │
│  │  │ Canvas     │  │  ┌─────┐ ┌──────┐ ┌─────────┐│  │    │
│  │  │ (Pixi.js)  │  │  │消息  │ │表情栏│ │快捷操作 ││  │    │
│  │  └────────────┘  │  └─────┘ └──────┘ └─────────┘│  │    │
│  └──────────────────┴────────────────────────────────┘  │    │
│                          │ HTTP                           │    │
│  ┌───────────────────────┴───────────────────────────┐    │    │
│  │  server.js v4.0 (fork 子进程, 127.0.0.1:8888)    │    │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐        │    │    │
│  │  │ 静态文件  │  │ 聊天代理  │  │ TTS 引擎 │        │    │    │
│  │  │ routes/  │  │ services/│  │ services/│        │    │    │
│  │  └──────────┘  └────┬─────┘  └─────┬────┘        │    │    │
│  │  ┌──────────────────┴──────────────┴─────────┐   │    │    │
│  │  │ lib/ (config, logger, utils)              │   │    │    │
│  │  └────────────────────────────────────────────┘   │    │    │
│  └────────────────────────────────────────────────────┘    │    │
└─────────────────────────────┬──────────────────────────────┘    │
                              │                                   │
                   ┌──────────▼──────┐  ┌──────▼──────────┐      │
                   │ OpenClaw Gateway│  │  GPT-SoVITS     │      │
                   │ :28789          │  │  :9880           │      │
                   └─────────────────┘  └─────────────────┘      │
                              │                                   │
                   ┌──────────▼──────┐                           │
                   │ Ollama          │                           │
                   │ :11434          │                           │
                   └─────────────────┘                           │
```

---

## 三、核心技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| **桌面壳** | Electron 28 | 窗口，系统托盘，子进程管理 |
| **前端** | 原生 HTML/CSS/JS | 无框架，Pixi.js v7 + pixi-live2d-display |
| **后端** | Node.js http 模块 | 无 Express，纯原生路由，模块化架构 |
| **AI 对话** | OpenClaw Gateway / Ollama | 可选后端，system prompt 注入 |
| **语音合成** | GPT-SoVITS v2 Pro | 本地推理，自定义角色音色，GPU 缓存管理 |
| **虚拟形象** | Live2D Cubism 3 | Pixi.js 渲染，鼠标追踪，表情切换 |
| **打包** | asar + electron-builder | NSIS 安装包 |

---

## 四、后端 API 端点 (server.js v4.0)

| 方法 | 路由 | 功能 |
|------|------|------|
| `GET` | `/api/health` | 服务器健康检查 (GPU/TTS/Ollama/GPU缓存状态) |
| `GET` | `/api/tts-latest` | 获取最新 TTS 音频 URL (轮询用，不限流) |
| `POST` | `/api/tts` | TTS 生成（返回原始音频） |
| `POST` | `/api/tts-generate` | TTS 生成（返回 JSON `{audio_url}`） |
| `GET/POST` | `/api/tts-config` | 获取/更新 TTS 参数 |
| `GET/POST` | `/api/tts-mode` | 切换 TTS 模式 (local/system/cloud) |
| `GET/POST` | `/api/gpt-sovits-config` | 获取/更新 GPT-SoVITS 连接配置 |
| `GET` | `/api/ref-audio-list` | 列出参考音频文件 |
| `POST` | `/api/tts-gc` | 手动触发 TTS GPU 缓存清理 |
| `GET` | `/api/tts-gc-status` | GPU 缓存状态 |
| `POST` | `/api/upload-file` | 文件上传 (图片/文档) |
| `POST` | `/api/upload-audio` | 音频上传 |
| `GET/POST` | `/api/chat-mode` | 切换聊天模式 |
| `GET` | `/api/config-reload` | 热重载配置文件 |
| `GET` | `/api/bug-reports` | 列出 Bug 报告 |
| `GET` | `/api/bug-report` | 查看单个 Bug 报告 |
| `POST` | `/api/clean-memory` | 内存清理 (TTS 队列 + 限流条目 + V8 GC) |
| `POST` | `/api/gateway/v1/chat/completions` | → 代理到 OpenClaw Gateway |
| `POST` | `/api/ollama/v1/chat/completions` | → 代理到本地 Ollama |
| `GET` | `/api/ollama/models` | 获取 Ollama 模型列表 |
| `GET` | `/api/gateway/*` | 通用 Gateway 路径代理 |
| `GET` | `/audio/*` | 静态音频文件服务 |

---

## 五、配置系统

项目有两个 config.json：

**根配置 `E:\openclaw\murasame\config.json`** (v3.1)
- 定义 `models` 数组（4 个 AI 后端预设：qclaw、ollama-local、custom-openai、custom-deepseek）
- 包含 `default_model` 指定默认模型
- 供 Electron 前端读取显示模型切换面板

**运行配置 `launcher/resources/app_current/config.json`** (v3.0)
- 实际后端运行时使用的配置
- 比根配置多了 `gpu_cache`、`tts.retry`、`tts.top_k/p/temperature/speed/cut_punc` 等详细参数
- 支持热重载 (`GET /api/config-reload`)

详细结构见 [launcher/resources/app_current/config.json](launcher/resources/app_current/config.json)。

---

## 六、启动方式

| 方式 | 命令 | 启动的服务 |
|------|------|-----------|
| **Python 启动** | `python start.py` | Ollama + GPT-SoVITS + Node 服务器 |
| **PowerShell** | `.\start.ps1` | GPT-SoVITS + Ollama + Electron |
| **开发模式 — 浏览器** | `cd launcher/resources/app_current && 启动浏览器版.bat` | 仅后端 + 浏览器打开 |
| **开发模式 — 桌面** | `cd launcher/resources/app_current && 启动桌面版.bat` | 后端 + 桌面浏览器 |
| **直接启动 exe** | `丛雨Live2D.exe` | Electron (内嵌 fork server.js) |

---

## 七、关键特性

1. **Live2D 虚拟形象** — 全屏渲染，鼠标追踪视线，点击互动，7 种表情 + 12 组动作
2. **AI 对话** — 支持 OpenClaw Gateway 和 Ollama 两种后端，system prompt 注入角色设定
3. **语音合成 (TTS)** — GPT-SoVITS 本地推理，自定义音色，队列管理，三次降级参数尝试
4. **GPU 缓存管理** — 跟踪生成次数，超限自动重启 GPT-SoVITS 释放显存；VRAM 超阈值触发清理
5. **Ollama 自动启动** — server.js 内置进程搜索、启动、崩溃自动恢复
6. **Bug 报告系统** — 自动捕获异常生成 JSON 报告（含内存快照、配置、日志）
7. **限流保护** — 每 IP 每分钟请求上限，/api/tts-latest 轮询豁免
8. **玻璃拟态 UI** — 樱花紫主题，毛玻璃效果，深色渐变背景，樱花飘落 + 光晕粒子特效
9. **GalGame 模式** — 视觉小说风格覆盖层，可切换
10. **Electron 桌面集成** — 系统托盘，子进程管理，自动启动外部服务

---

## 八、已知问题

- 记忆系统为**前端纯存根**（`openclaw-features.js`），未连接后端 API
- 技能/日程/提醒面板的 UI 部分为存根，未连接实际业务逻辑
- 记忆检索基于**关键词匹配**，非语义向量搜索
- `start.py` 和 `start.ps1` 仍引用已删除的 `src/` 路径，需更新
- GPT-SoVITS 长时间运行会产生显存泄漏，GPU 缓存清理机制可缓解但无法根治
